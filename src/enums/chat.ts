export enum EChatType {
  PRIVATE = 1,
  GROUP = 2,
}

export enum EMessageStatus {
  SENT = 1,
  DELIVERED = 2,
  READ = 3,
}

export enum EMessageType {
  TEXT = 1,
  IMAGE = 2,
  FILE = 3,
}
