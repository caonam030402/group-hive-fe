"use client";

import React from "react";

import Header from "@/components/layouts/Header";

export default function HomePage() {
  return (
    <div className="">
      <Header />
      {/* <Button
        onClick={() => {
          signOut();
        }}
        color="primary"
      >
        Button
      </Button> */}
    </div>
  );
}
